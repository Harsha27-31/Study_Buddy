<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>StudyBuddy | Dashboard</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

<style>
  :root {
    --primary: #6366f1;
    --primary-light: #818cf8;
    --primary-dark: #4f46e5;
    --secondary: #f59e0b;
    --secondary-light: #fbbf24;
    --dark: #1e293b;
    --darker: #0f172a;
    --light: #f8fafc;
    --lighter: #ffffff;
    --gray: #94a3b8;
    --gray-light: #e2e8f0;
    --success: #10b981;
    --danger: #ef4444;
    --glass: rgba(255, 255, 255, 0.15);
    --glass-border: rgba(255, 255, 255, 0.2);
    --card-bg: rgba(255, 255, 255, 0.98);
    --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.05);
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
    --shadow-md: 0 10px 15px -3px rgba(0, 0, 0, 0.06), 0 4px 6px -2px rgba(0, 0, 0, 0.03);
    --shadow-lg: 0 20px 25px -5px rgba(0, 0, 0, 0.08), 0 10px 10px -5px rgba(0, 0, 0, 0.02);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Inter', sans-serif;
  }

  body {
    background: #f9fafb;
    color: var(--dark);
    min-height: 100vh;
    line-height: 1.5;
    overflow-x: hidden;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 2rem 2.5%;
  }

  /* Modern Header */
  .dashboard-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2.5rem;
    padding: 1.5rem 2rem;
    background: var(--lighter);
    border-radius: 12px;
    box-shadow: var(--shadow);
    animation: fadeInDown 0.6s ease-out;
    border: 1px solid var(--gray-light);
  }

  .header-left {
    display: flex;
    align-items: center;
    gap: 1.2rem;
  }

  .header-icon {
    width: 50px;
    height: 50px;
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.4rem;
  }

  .header-text h1 {
    font-size: 1.6rem;
    font-weight: 700;
    color: var(--darker);
    line-height: 1.3;
  }

  .user-info {
    font-size: 0.9rem;
    color: var(--gray);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.3rem;
  }

  .logout-btn {
    background: var(--lighter);
    color: var(--primary);
    border: 1px solid var(--gray-light);
    padding: 0.7rem 1.5rem;
    border-radius: 8px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    gap: 0.6rem;
    box-shadow: var(--shadow-sm);
  }

  .logout-btn:hover {
    background: var(--primary);
    color: white;
    border-color: var(--primary);
  }

  /* Stats Cards - Modern Minimal */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2.5rem;
  }

  .stat-card {
    background: var(--lighter);
    border-radius: 12px;
    padding: 1.5rem;
    box-shadow: var(--shadow);
    transition: all 0.2s ease;
    border: 1px solid var(--gray-light);
    position: relative;
    overflow: hidden;
  }

  .stat-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
  }

  .stat-icon {
    width: 44px;
    height: 44px;
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;
    margin-bottom: 1rem;
  }

  .stat-value {
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--darker);
    margin-bottom: 0.3rem;
  }

  .stat-label {
    font-size: 0.9rem;
    color: var(--gray);
    font-weight: 500;
  }

  /* Study Tip - Clean Design */
  .study-tip {
    background: var(--lighter);
    border-radius: 12px;
    padding: 1.5rem;
    margin-bottom: 2.5rem;
    box-shadow: var(--shadow);
    border: 1px solid var(--gray-light);
    position: relative;
  }

  .study-tip h3 {
    font-size: 1.1rem;
    color: var(--darker);
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .study-tip h3 i {
    color: var(--secondary);
  }

  .study-tip p {
    color: var(--dark);
    font-size: 0.9rem;
    line-height: 1.6;
  }

  /* Subjects Section - Modern Layout */
  .subjects-section {
    background: var(--lighter);
    border-radius: 12px;
    padding: 2rem;
    box-shadow: var(--shadow);
    margin-bottom: 2.5rem;
    border: 1px solid var(--gray-light);
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 1.5rem;
  }

  .section-title {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--darker);
    position: relative;
  }

  .section-link {
    color: var(--primary);
    font-weight: 500;
    text-decoration: none;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.9rem;
    transition: all 0.2s ease;
  }

  .section-link:hover {
    color: var(--primary-dark);
  }

  /* Modern Subjects List */
  .subjects-container {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .branch-section {
    margin-bottom: 2rem;
  }

  .branch-title {
    font-size: 1.2rem;
    font-weight: 600;
    color: var(--darker);
    margin-bottom: 1.2rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--gray-light);
  }

  .year-group {
    margin-bottom: 1.5rem;
  }

  .year-title {
    font-weight: 600;
    color: var(--dark);
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 1rem;
  }

  .semester-group {
    margin-left: 1rem;
    margin-bottom: 1rem;
  }

  .semester-title {
    font-weight: 500;
    color: var(--dark);
    margin-bottom: 0.5rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.95rem;
  }

  .subjects-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-left: 1.5rem;
  }

  .subject-tag {
    display: inline-block;
    padding: 0.3rem 0.8rem;
    background: rgba(99, 102, 241, 0.08);
    border-radius: 6px;
    font-size: 0.75rem;
    font-weight: 500;
    color: var(--primary-dark);
    transition: all 0.2s ease;
    border: 1px solid rgba(99, 102, 241, 0.2);
  }

  .subject-tag:hover {
    background: rgba(99, 102, 241, 0.15);
    transform: translateY(-1px);
  }

  /* Action Buttons - Modern Style */
  .action-buttons {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin-top: 3rem;
    flex-wrap: wrap;
  }

  .action-btn {
    padding: 0.9rem 1.8rem;
    border-radius: 8px;
    font-weight: 500;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    transition: all 0.2s ease;
  }

  .primary-btn {
    background: linear-gradient(135deg, var(--primary), var(--primary-dark));
    color: white;
    box-shadow: var(--shadow);
  }

  .primary-btn:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .secondary-btn {
    background: var(--lighter);
    color: var(--primary);
    border: 1px solid var(--gray-light);
    box-shadow: var(--shadow-sm);
  }

  .secondary-btn:hover {
    background: rgba(99, 102, 241, 0.05);
    transform: translateY(-2px);
    border-color: var(--primary-light);
  }

  /* Animations */
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fadeInDown {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .container {
      padding: 1.5rem;
    }
    
    .dashboard-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1.2rem;
      padding: 1.2rem;
    }
    
    .stats-grid {
      grid-template-columns: 1fr;
    }
    
    .subjects-section {
      padding: 1.5rem;
    }
    
    .action-buttons {
      flex-direction: column;
      gap: 1rem;
    }
    
    .action-btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
</head>
<body>
  <div class="container">
    <!-- Modern Header -->
    <header class="dashboard-header">
      <div class="header-left">
        <div class="header-icon">
          <i class="fas fa-user-graduate"></i>
        </div>
        <div class="header-text">
          <h1>Welcome back, <%= user.userName %> <span class="welcome-emoji">👋</span></h1>
          <div class="user-info">
            <i class="fas fa-calendar-alt"></i>
            <span><%= new Date().toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' }) %></span>
          </div>
        </div>
      </div>

      <form action="/logout" method="POST">
        <button type="submit" class="logout-btn">
          <i class="fas fa-sign-out-alt"></i>
          <span>Logout</span>
        </button>
      </form>
    </header>

    <!-- Stats Cards -->
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-book-open"></i></div>
        <div class="stat-value"><%= stats.subjects || 0 %></div>
        <div class="stat-label">Subjects Enrolled</div>
      </div>
      <div class="stat-card">
        <div class="stat-icon"><i class="fas fa-file-alt"></i></div>
        <div class="stat-value"><%= stats.notes || 0 %></div>
        <div class="stat-label">Notes Uploaded</div>
      </div>
    </div>

    <!-- Study Tip of the Day -->
    <div class="study-tip">
      <h3><i class="fas fa-lightbulb"></i> Study Tip of the Day</h3>
      <p>The Pomodoro Technique: Study for 25 minutes, then take a 5-minute break. After four cycles, take a longer 15-30 minute break. This method helps maintain focus and prevents burnout.</p>
    </div>

    <!-- Subjects Section - Modern Layout -->
    <section class="subjects-section">
      <div class="section-header">
        <h2 class="section-title">Your Subjects</h2>
        <a href="/subjects" class="section-link">
          <span>View All</span>
          <i class="fas fa-arrow-right"></i>
        </a>
      </div>

      <div class="subjects-container">
        <!-- First Year -->
        <div class="branch-section">
          <h3 class="branch-title">First Year</h3>
          
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> Semester 1</h4>
            <div class="subjects-list">
              <% ["M1", "CHEMISTRY", "BCME", "ENGLISH", "CP", "IT WORK SHOP", "CHEMISTRY LAB", "ENGINEERING WORKSHOP", "CP LAB", "YOGA & SPORTS"].forEach(subject => { %>
                <span class="subject-tag"><%= subject %></span>
              <% }) %>
            </div>
          </div>
          
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> Semester 2</h4>
            <div class="subjects-list">
              <% ["M2", "PHYSICS", "BEEE", "CS LAB", "EG-DRAWING", "PHYSICS LAB", "BEEE LAB", "DS", "DS LAB", "NSS"].forEach(subject => { %>
                <span class="subject-tag"><%= subject %></span>
              <% }) %>
            </div>
          </div>
        </div>

        <!-- Computer Science (CSE) -->
        <div class="branch-section">
          <h3 class="branch-title">Computer Science (CSE)</h3>
          
          <!-- 2nd Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 2nd Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["DSA", "OOP", "DBMS", "COA", "DMS"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 2</h5>
              <div class="subjects-list">
                <% ["OS", "CN", "SE", "TOC", "DAA"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
          
          <!-- 3rd Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 3rd Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["ML", "CD", "CC", "IOT", "AI"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 2</h5>
              <div class="subjects-list">
                <% ["BD", "NLP", "IS", "CG", "SPM"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
          
          <!-- 4th Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 4th Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["BC", "DL", "ES", "PP", "DA"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
        </div>

        <!-- AI & Machine Learning (AIML) -->
        <div class="branch-section">
          <h3 class="branch-title">AI & Machine Learning (AIML)</h3>
          
          <!-- 2nd Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 2nd Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["DSA", "OOP", "DBMS", "COA", "DMS"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 2</h5>
              <div class="subjects-list">
                <% ["OS", "CN", "SE", "TOC", "DAA"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
          
          <!-- 3rd Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 3rd Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["ML", "DL", "AI", "IOT", "CV"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 2</h5>
              <div class="subjects-list">
                <% ["NLP", "BD", "IS", "CG", "SPM"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
          
          <!-- 4th Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 4th Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["BC", "RL", "ES", "PP", "DA"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
        </div>

        <!-- AI & Data Science (AIDS) -->
        <div class="branch-section">
          <h3 class="branch-title">AI & Data Science (AIDS)</h3>
          
          <!-- 2nd Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 2nd Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["DSA", "OOP", "DBMS", "COA", "DMS"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["ML", "BD", "AI", "IOT", "DA"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 2</h5>
              <div class="subjects-list">
                <% ["NLP", "DL", "IS", "CG", "SPM"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
          
          <!-- 4th Year -->
          <div class="year-group">
            <h4 class="year-title"><i class="fas fa-calendar-alt"></i> 4th Year</h4>
            
            <div class="semester-group">
              <h5 class="semester-title"><i class="fas fa-book"></i> Semester 1</h5>
              <div class="subjects-list">
                <% ["BC", "RL", "ES", "PP", "CV"].forEach(subject => { %>
                  <span class="subject-tag"><%= subject %></span>
                <% }) %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Action Buttons -->
    <div class="action-buttons">
      <a href="/upload" class="action-btn primary-btn">
        <i class="fas fa-book"></i>
        <span>View My Uploads</span>
      </a>
      <a href="/upload" class="action-btn secondary-btn">
        <i class="fas fa-cloud-upload-alt"></i>
        <span>Upload New Notes</span>
      </a>
    </div>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      // 1. Animation for cards with staggered delay
      const animateElements = () => {
        const elements = document.querySelectorAll('.stat-card, .branch-section');
        
        elements.forEach((el, index) => {
          setTimeout(() => {
            el.style.opacity = '1';
            el.style.transform = 'translateY(0)';
            el.style.transition = 'opacity 0.4s ease, transform 0.4s ease';
          }, index * 150);
        });
      };
  
      // 2. User avatar initialization
      const initUserAvatar = () => {
        const username = '<%= user.userName %>';
        const avatar = document.createElement('div');
        avatar.className = 'avatar-circle';
        avatar.textContent = username.charAt(0).toUpperCase();
        
        const userInfoContainer = document.querySelector('.header-left');
        if (userInfoContainer) {
          userInfoContainer.appendChild(avatar);
        }
      };
  
      // 3. Ripple effect for buttons
      const addRippleEffect = () => {
        const buttons = document.querySelectorAll('.logout-btn, .action-btn');
        
        buttons.forEach(button => {
          button.addEventListener('click', function(e) {
            const rect = this.getBoundingClientRect();
            const x = e.clientX - rect.left;
            const y = e.clientY - rect.top;
            
            const ripple = document.createElement('span');
            ripple.className = 'ripple-effect';
            ripple.style.left = `${x}px`;
            ripple.style.top = `${y}px`;
            
            this.appendChild(ripple);
            
            setTimeout(() => {
              ripple.remove();
            }, 600);
          });
        });
      };
  
      // 4. Responsive adjustments
      const handleResponsive = () => {
        if (window.innerWidth < 768) {
          document.querySelectorAll('.subjects-list').forEach(list => {
            list.style.marginLeft = '0.5rem';
          });
        }
      };
  
      // 5. Initialize all functions
      const init = () => {
        animateElements();
        initUserAvatar();
        addRippleEffect();
        handleResponsive();
        
        // Add resize listener for responsive adjustments
        window.addEventListener('resize', handleResponsive);
      };
  
      init();
    });
  
    // Ripple effect animation
    const style = document.createElement('style');
    style.textContent = `
      @keyframes ripple {
        to {
          transform: scale(4);
          opacity: 0;
        }
      }
      .ripple-effect {
        position: absolute;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.7);
        transform: scale(0);
        animation: ripple 0.6s linear;
        pointer-events: none;
      }
      .avatar-circle {
        width: 40px;
        height: 40px;
        background-color: #6366f1;
        color: white;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 600;
        margin-left: 1rem;
      }
    `;
    document.head.appendChild(style);
  </script>
</body>
</html>